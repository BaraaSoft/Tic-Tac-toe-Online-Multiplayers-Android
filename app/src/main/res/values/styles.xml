<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="background">@color/background</item>
        <item name="android:actionMenuTextColor">@color/colorPrimary</item>
        <item name="android:actionBarItemBackground">@color/colorPrimary</item>
    </style>
    <style name="GameTheme" parent="AppTheme">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:textColor">#CC213E</item>
        <item name="android:fontFamily">@font/frijole</item>
    </style>
    <style name="ListItemTheme" parent="AppTheme">
        <item name="android:textColor">@color/primary_text_light</item>
        <item name="android:background">@color/light_separator</item>
    </style>



</resources>
